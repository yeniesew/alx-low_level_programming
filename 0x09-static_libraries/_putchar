#include<unistd.h>
/**
* _putchar - writes the character to stdout
* @c: the character to print
* Return: on success 1.
* on error return, -1 is returned, and error no is set appropratly
*/
int _putchar(char c)
{
return (write(1, &c, 1));
}
